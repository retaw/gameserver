#include "msg_queue.h"

namespace water{
namespace net{

PacketQueue::PacketQueue(uint32_t circularSize, uint32_t extendSizeLimit)
: m_circularQueue(circularSize), m_extendQueue(circularSize)
{
}

PacketQueue


}}
